<%= form_for current_user.following_relations.where(:followed_id => @user.id).first,
             :html => { :method => :delete }, :remote => true do |f| %>
  <div class="actions"><%= f.submit "Unfollow" %></div>
<% end %>